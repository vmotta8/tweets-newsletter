service:
  name: user-service

plugins:
  - serverless-bundle
  - serverless-pseudo-parameters

provider:
  name: aws
  runtime: nodejs12.x
  memorySize: 256
  stage: ${opt:stage, 'dev'}
  region: eu-west-1
  
functions:
  hello:
    handler: dist/handlers/subscribe.handler
    events:
      - http:
          method: POST
          path: /subscribe

custom:
  bundle:
    linting: false